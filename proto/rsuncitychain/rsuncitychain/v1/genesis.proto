syntax = "proto3";
package rsuncitychain.rsuncitychain.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "rsuncitychain/rsuncitychain/v1/params.proto";

option go_package = "rsuncitychain/x/rsuncitychain/types";

// GenesisState defines the rsuncitychain module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
